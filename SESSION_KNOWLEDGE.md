# Session Knowledge — 3D Demon Timeline

Template project built with React + Vite + Three.js. Edit `src/data/demons.json` to add your own demons, and `src/timeline.config.js` to change the title/about text.
