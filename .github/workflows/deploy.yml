name: Deploy to Cloudflare Pages

on:
  push:
    branches: [Main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
        env:
          VITE_APP_KEY: gd-timeline-2024
          VITE_SUPABASE_URL: https://bmjpjuktqaupeuxqqmaq.supabase.co
          VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJtanBqdWt0cWF1cGV1eHFxbWFxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDkyMDY0MzEsImV4cCI6MjA2NDc4MjQzMX0.0rrCnEaSKK9IQCFBJ5HkBxlxNMFSzMLD9ldR_Pq-F1I
      - run: npx wrangler pages deploy dist --project-name gd-demon-timeline
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: a6251e3a709ffa39470050c6947828a7
